var sx = document.getElementById('sx');        // screenX 속성 값을 보여줄 요소var sy = document.getElementById('sy');        // screenY 속성 값을 보여줄 요소var px = document.getElementById('px');        // pageX 속성 값을 보여줄 요소var py = document.getElementById('py');        // pageY 속성 값을 보여줄 요소var cx = document.getElementById('cx');        // clientX 속성 값을 보여줄 요소var cy = document.getElementById('cy');        // clientY 속성 값을 보여줄 요소function showPosition(event) {                 // 함수를 선언한다  sx.value = event.screenX;                    // screenX 값을 출력한다  sy.value = event.screenY;                    // screenY 값을 출력한다  px.value = event.pageX;                      // pageX 값을 출력한다  py.value = event.pageY;                      // pageY 값을 출력한다  cx.value = event.clientX;                    // clientX 값을 출력한다  cy.value = event.clientY;                    // clientY 값을 출력한다}var el = document.getElementById('body');      // body 요소를 가져온다el.addEventListener('mousemove', showPosition, false); // 이벤트를 바인딩한다